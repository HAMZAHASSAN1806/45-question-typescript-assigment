"use strict";
// SOMETHING STORE IN A ARRAY
/* Qustion.20 Something store in a Array: Think of something you could store in a array. For example, you could make a list of
mountains, rivers, countries, cities, languages, or anything else you’d like. Write a
program that creates a list containing these items. */
let cities = ["Karachi", "Lahore", "Peshawar", "Quetta", "Multan"];
cities.map((item) => console.log(`${item}`));
